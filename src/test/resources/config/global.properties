# Framework defaults. Environment files (dev/stag/prod.properties) override these,
# then config/local.properties (git-ignored), then environment variables, then -D flags.

request.timeout.seconds=30
retry.429.max.attempts=3
log.requests=true
