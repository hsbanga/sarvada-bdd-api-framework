# Named endpoints used by feature files. Path parameters use {name} and are filled from "PATH".
# Values are relative to api.<alias>.base.url configured in config/<env>.properties.

USERS=/users
USER_BY_ID=/users/{id}
POSTS=/posts
POST_BY_ID=/posts/{id}
POST_COMMENTS=/posts/{id}/comments
COMMENTS=/comments
